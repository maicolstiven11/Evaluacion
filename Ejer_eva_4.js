import { procesarImpuestos } from "./modulos/Ejer_4.js";  //importo el modulo necesario para el ejericio numero 4
let total = procesarImpuestos();  // creo una variable llamda total y le asigno el valor que retorne la funcion

// Muesto el resultado en consola de el valor que retorno la funcion que se guardo en total
console.log("Impuesto total acumulado:", total); 
